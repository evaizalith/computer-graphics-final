#ifndef EVA_TEXTURED_OBJECTS
#define EVA_TEXTURED_OBJECTS

#include <GL/gl.h>

void eglCube(float l, float w, float h, unsigned int texture_id);
void eglSphere(float size, unsigned int texture_id);

#endif